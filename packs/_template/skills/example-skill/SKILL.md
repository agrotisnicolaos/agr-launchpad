---
name: example-skill
description: Replace this with a real skill. Describe precisely WHEN to use it (trigger phrases, situations) so Claude invokes it at the right time. This example exists only to show the file shape a pack skill must take.
---

This is the body of the skill — the instructions Claude follows when the skill is invoked.

Write it as direct guidance: what to do, in what order, what to avoid. Keep it focused on one
job. See the base skills in `.claude/skills/` (coding-standards, error-handling, html, …) for
real examples of tone and structure.

Delete this file and add your own under `packs/<your-pack>/skills/<name>/SKILL.md`.
