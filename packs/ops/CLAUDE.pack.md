## Pack: Ops / Observability

When the task is a live monitoring dashboard on Grafana, SigNoz, or a similar platform, use the
`dashboard-builder` skill — start from operator questions, not visual layout. For standalone,
no-server dashboards or one-off data visualizations, prefer the base `html` skill instead.
